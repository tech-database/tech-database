# 无限层级分类系统 - 实现计划（分解和优先级任务列表）

## [x] 任务 1：更新类型定义
- **优先级**：P0
- **依赖**：无
- **描述**：
  - 更新 `src/types/index.ts` 文件，将现有的两级分类接口替换为支持无限层级的 `Category` 接口
  - 更新 `FileItem` 接口，将 `category_level1_id` 和 `category_level2_id` 替换为 `category_id`
  - 添加 `FileWithCategories` 接口，支持分类路径信息
- **验收标准**：AC-1, AC-6
- **测试要求**：
  - `programmatic` TR-1.1: 类型定义正确，编译无错误
  - `human-judgment` TR-1.2: 类型定义清晰，符合无限层级分类的需求
- **备注**：确保所有引用这些类型的文件都能正确更新

## [x] 任务 2：创建数据库迁移脚本
- **优先级**：P0
- **依赖**：任务 1
- **描述**：
  - 创建 `complete-database-setup.sql` 脚本
  - 包含创建 `categories` 表、更新 `files` 表、创建存储桶和权限策略的 SQL 语句
  - 包含初始分类数据的插入语句
- **验收标准**：AC-6
- **测试要求**：
  - `programmatic` TR-2.1: 脚本执行无错误
  - `programmatic` TR-2.2: 数据库表结构正确创建
- **备注**：脚本需要在 Supabase 控制台执行

## [x] 任务 3：更新分类树组件
- **优先级**：P0
- **依赖**：任务 1
- **描述**：
  - 修改 `src/components/CategoryTree.tsx` 组件
  - 实现递归渲染，支持无限层级的分类树
  - 保持现有的样式和交互方式
- **验收标准**：AC-3
- **测试要求**：
  - `human-judgment` TR-3.1: 分类树正确显示多层级分类
  - `human-judgment` TR-3.2: 展开/折叠功能正常工作
- **备注**：确保组件能够处理深层级的分类结构

## [x] 任务 4：更新分类管理页面
- **优先级**：P0
- **依赖**：任务 1, 任务 3
- **描述**：
  - 修改 `src/pages/CategoryManagementPage.tsx` 页面
  - 实现添加任意层级分类的功能
  - 实现分类的删除功能
  - 支持分类树的展开/折叠
- **验收标准**：AC-1, AC-2
- **测试要求**：
  - `human-judgment` TR-4.1: 能够添加顶级分类和子分类
  - `human-judgment` TR-4.2: 能够删除分类及其子分类
- **备注**：添加适当的错误处理和用户提示

## [x] 任务 5：更新分类浏览页面
- **优先级**：P0
- **依赖**：任务 1, 任务 3
- **描述**：
  - 修改 `src/pages/CategoryBrowsePage.tsx` 页面
  - 实现基于 `selectedCategory` 的文件查询
  - 支持点击任意层级分类查看文件
  - 保持现有的批量操作功能
- **验收标准**：AC-3
- **测试要求**：
  - `human-judgment` TR-5.1: 点击分类树中的分类能够正确显示对应文件
  - `human-judgment` TR-5.2: 批量操作功能正常工作
- **备注**：修复可能的变量引用错误

## [x] 任务 6：更新文件上传页面
- **优先级**：P0
- **依赖**：任务 1
- **描述**：
  - 修改 `src/pages/FileUploadPage.tsx` 页面
  - 实现基于新分类结构的分类选择
  - 保持现有的文件上传功能
- **验收标准**：AC-4
- **测试要求**：
  - `human-judgment` TR-6.1: 能够选择任意层级的分类
  - `human-judgment` TR-6.2: 文件上传功能正常工作
- **备注**：确保分类选择下拉框能够显示所有分类

## [x] 任务 7：更新文件卡片组件
- **优先级**：P0
- **依赖**：任务 1
- **描述**：
  - 修改 `src/components/FileCard.tsx` 组件
  - 实现显示完整分类路径的功能
  - 保持现有的文件信息显示和操作功能
- **验收标准**：AC-5
- **测试要求**：
  - `human-judgment` TR-7.1: 文件卡片正确显示分类路径
  - `human-judgment` TR-7.2: 其他文件信息显示正常
- **备注**：确保分类路径显示美观，不会溢出

## [x] 任务 8：测试和验证
- **优先级**：P1
- **依赖**：所有任务
- **描述**：
  - 执行数据库迁移脚本
  - 测试所有功能，确保系统正常工作
  - 检查错误处理和用户提示
- **验收标准**：所有 AC
- **测试要求**：
  - `human-judgment` TR-8.1: 所有功能正常工作
  - `human-judgment` TR-8.2: 错误处理和用户提示完善
- **备注**：测试深层级分类结构的性能和稳定性